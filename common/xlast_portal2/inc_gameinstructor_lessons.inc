// Portal 2 port: game instructor lessons: CFG( lesson ), in title data (GI.lesson.<lesson>) order.
// Reconstructed from the retail Portal 2 Linux matchmaking.so title data tables (order and values as
// shipped). Not original Valve source; the repository's provenance and
// distribution warning applies.
CFG( version_number )
CFG( return_to_hub )
CFG( create_left_portal )
CFG( create_right_portal )
CFG( movement )
CFG( push_button )
CFG( countdown_timer )
CFG( air_taunt )
CFG( new_taunt_earned )
CFG( zoom )
CFG( unzoom )
CFG( jump )
CFG( movement_sp )
CFG( drop_object )
CFG( pick_up_object )
CFG( partner_view )
CFG( serverside_hint )
