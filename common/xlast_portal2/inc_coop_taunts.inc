// Portal 2 port: co-op taunts: CFG( taunt ), in title data (MP.taunt.own/use.<taunt>) order.
// Reconstructed from the retail Portal 2 Linux matchmaking.so title data tables (order and values as
// shipped). Not original Valve source; the repository's provenance and
// distribution warning applies.
CFG( SmallWave )
CFG( RobotDance )
CFG( Laugh )
CFG( TrickFire )
CFG( HighFive )
CFG( TeamHug )
CFG( TeamTease )
CFG( RPS )
