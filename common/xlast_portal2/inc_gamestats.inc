// Portal 2 port: game stats: CFG( name, settings type, C++ type ), in title data (GAME.<name>) order. The settings type is the console profile type, unused on this platform; uint32 matches the other counters.
// Reconstructed from the retail Portal 2 Linux matchmaking.so title data tables (order and values as
// shipped). Not original Valve source; the repository's provenance and
// distribution warning applies.
CFG( stat_player_steps, 0, uint32 )
CFG( stat_portals_placed, 0, uint32 )
CFG( stat_portals_traveled, 0, uint32 )
