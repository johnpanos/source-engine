// Portal 2 port: game modes for the rich presence context: CFG( mode ).
// Reconstructed from the retail Portal 2 Linux matchmaking.so table (names and
// order as shipped; CONTEXT_GAME_MODE_<mode> values in portal2.spa.h). Not
// original Valve source; the repository's provenance and distribution
// warning applies.
CFG( SP )
CFG( COOP )
CFG( COOP_CHALLENGE )
CFG( COOP_COMMUNITY )
