// vstcheckoutfilePlugin
// Initial code generated by XSI SDK Wizard
// Executed Fri Dec 16 10:40:31 PST 2005 by stevek
// 
// Tip: To add a command to this plug-in, right-click in the 
// script editor and choose Tools > Add Command.
// 
// Tip: To get help on a callback, highlight the callback name
// (for example, "Init", "Define", or "Execute") and press F1.

function XSILoadPlugin( in_reg )
{
	in_reg.Author = "stevek";
	in_reg.Name = "vstcheckoutfilePlugin";
	in_reg.Email = "";
	in_reg.URL = "";
	in_reg.Major = 1;
	in_reg.Minor = 0;

	in_reg.RegisterCommand("vstcheckoutfile","vstcheckoutfile");
	//RegistrationInsertionPoint - do not remove this line

	return true;
}

function XSIUnloadPlugin( in_reg )
{
	strPluginName = in_reg.Name;
	Application.LogMessage(strPluginName + " has been unloaded.");
	return true;
}


function vstcheckoutfile_Init( ctxt )
{
	var oCmd;
	oCmd = ctxt.Source;
	oCmd.Description = "Pass it a filename and the command checks out the file if needed";
	oCmd.Tooltip = "undefined";
	oCmd.ReturnValue = true;

	var oArgs;
	oArgs = oCmd.Arguments;
	oArgs.Add("filename",siArgumentInput);
	return true;
}

function vstcheckoutfile_Execute( filename )
{
	Application.LogMessage("vstcheckoutfile_Execute called");
	checkoutfile( filename );
	return true;
}


/*******************************
check out file command
********************************/

function checkoutfile( filename )
{
					logmessage ("checking out " + filename);
					shell = new ActiveXObject("WScript.Shell");
					p4 = "p4 edit " + filename;
 					shell.Run(p4,0,true);
}