// vstFixHandBonesPlugin
// Initial code generated by XSI SDK Wizard
// Executed Wed Nov 9 17:11:18 PST 2005 by stevek
// 
// Tip: To add a command to this plug-in, right-click in the 
// script editor and choose Tools > Add Command.
// 
// Tip: To get help on a callback, highlight the callback name
// (for example, "Init", "Define", or "Execute") and press F1.

function XSILoadPlugin( in_reg )
{
	in_reg.Author = "stevek";
	in_reg.Name = "vstFixHandBonesPlugin";
	in_reg.Email = "";
	in_reg.URL = "";
	in_reg.Major = 1;
	in_reg.Minor = 0;

	in_reg.RegisterCommand("vstFixHandBones","vstFixHandBones");
	//RegistrationInsertionPoint - do not remove this line

	return true;
}

function XSIUnloadPlugin( in_reg )
{
	strPluginName = in_reg.Name;
	Application.LogMessage(strPluginName + " has been unloaded.");
	return true;
}

function vstFixHandBones_Init( ctxt )
{
	var oCmd;
	oCmd = ctxt.Source;
	oCmd.Description = "";
	oCmd.ReturnValue = true;

	return true;
}

function vstFixHandBones_Execute(  )
{
	logmessage ("Running");
	fingers = SelectObj("*Finger*");
	f = GetValue("SelectionList");
	fix = ".kine.local.rotorder";
	for (i=0;i<f.count;i++)
	{
		bone = (f(i) + fix);
		SetValue(bone, 5, null);
	}
	DeselectAll();
}

